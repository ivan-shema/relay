NEXT_PUBLIC_API_URL=http://localhost:4000

# Google sign-in — same OAuth 2.0 Web client ID as GOOGLE_CLIENT_ID in
# apps/api/.env. Leave empty to hide the "Continue with Google" button.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
